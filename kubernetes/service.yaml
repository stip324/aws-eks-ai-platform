apiVersion: v1
kind: Service
metadata:
  name: vehicle-api
  namespace: vehicle-platform
spec:
  selector:
    app: vehicle-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
